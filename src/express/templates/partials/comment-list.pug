h2.comments__title.title.title--middle Комментарии
ul.comments__list
  each comment in article.comments
    li.comments__comment(id=comment.id)
    .comments__avatar.avatar
      img(src=`/img/${comment.users.avatar ? comment.users.avatar : `/icons/smile.svg`}` alt='аватар пользователя')
    .comments__text
      .comments__head
        p #{comment.users.name} #{comment.users.surname} •
        time.comments__date(datetime=comment.createdAt) #{formatDateComment(comment.createdAt)}
      p.comments__message #{comment.text}
